from django.apps import AppConfig


class OffersAppConfig(AppConfig):
    """Configuration for the offers app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "offers_app"
    verbose_name = "Offers"
